# Punchline - Cloudflare Pages Configuration
name = "punchline"
compatibility_date = "2024-09-09"

# Pages project configuration
pages_build_output_dir = "dist"

# D1 Database binding
[[d1_databases]]
binding = "DB"
database_name = "punchline-db"
database_id = "placeholder-replace-with-actual-id"
